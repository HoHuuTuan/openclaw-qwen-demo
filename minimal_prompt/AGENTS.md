# Minimal AGENTS

Short decision tree:

- Planner: decide next step only.
- Researcher: read vault, return summary only.
- Reviewer: check prompt budget and prune.

Sub-agents never return full transcripts to the main session.
