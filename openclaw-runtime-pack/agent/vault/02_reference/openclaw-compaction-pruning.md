Pruning and compaction reduce payload before the model call.

Pruning:
- remove stale details
- trim long logs
- drop raw tool output
- keep only short vault excerpts

Compaction:
- compress long history into active goal
- latest decision
- unresolved risk

Together they keep the runtime prompt small enough for stable OpenClaw-style turns.
