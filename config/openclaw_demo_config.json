{
  "demo_name": "openclaw-qwen-demo",
  "default_mode": "mock",
  "optional_model_mode": "ollama",
  "ollama_default_model": "qwen2.5:3b",
  "target_port": 3000,
  "notes": [
    "Mock mode is the default and does not require any API key.",
    "USE_OLLAMA=true enables local Ollama fallback, but the demo still falls back to mock if Ollama is unavailable.",
    "The goal is to demonstrate context optimization strategy, not cloud model connectivity."
  ]
}
