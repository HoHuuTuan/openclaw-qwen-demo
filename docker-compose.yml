services:
  openclaw-qwen-demo:
    build: .
    ports:
      - "3000:3000"
    environment:
      PYTHONUNBUFFERED: "1"
      GEMINI_API_KEY: "${GEMINI_API_KEY:-}"
      GOOGLE_API_KEY: "${GOOGLE_API_KEY:-}"
