services:
  openclaw-qwen-demo:
    build: .
    ports:
      - "3000:3000"
    environment:
      PYTHONUNBUFFERED: "1"
      USE_OLLAMA: "${USE_OLLAMA:-false}"
      OLLAMA_MODEL: "${OLLAMA_MODEL:-qwen2.5:3b}"
